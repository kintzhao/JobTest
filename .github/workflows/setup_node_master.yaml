name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: ./.github/actions/my-action
    - name: pwd && ls
      run: pwd && ls
    - name: cmake
      run: cmake
    - name: make
      run: make
    - name: pwd && ls
      run: pwd && ls
